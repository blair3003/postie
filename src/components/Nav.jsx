const Nav = ({ className }) => {

    return (
        <nav className={className}>
            <a href="#">Posts</a>
            <a href="#">Users</a>
            <a href="#">Other</a>
        </nav>
    )
}

export default Nav
const Nav = ({ className }) => {

    return (
        <nav className={className}>
            <a href="#">Login</a>
            <a href="/register">Register</a>
        </nav>
    )
}

export default Nav